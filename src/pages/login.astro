---
import Layout from "@/layouts/Layout.astro"
import LoginForm from "@/components/LoginForm"
---

<Layout>
  <main slot="login" class="flex max-md:flex-col min-h-screen justify-center">
    <div class="md:flex-1 flex items-center justify-center">
      <div
        class="bg-gradient-to-br from-indigo-600 via-indigo-400 to-indigo-600 p-3 rounded-full"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="size-48 text-white"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"
          ></path>
        </svg>
      </div>
    </div>

    <div class="w-1 flex items-center max-md:hidden">
      <div class="w-full bg-primary h-4/5 rounded-xl"></div>
    </div>

    <div class="md:flex-1 flex items-center justify-center">
      <div class="max-w-sm w-full px-5">
        <h1 class="text-3xl font-bold text-primary text-center">
          Bienvenido de nuevo!
        </h1>
        <p class="text-gray-300 text-center mb-4">
          Ingresa tu correo y contraseña para ingresar 😊
        </p>
        <LoginForm client:load />
      </div>
    </div>
  </main>
</Layout>
